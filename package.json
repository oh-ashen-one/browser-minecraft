{
  "name": "cubeland",
  "private": true,
  "version": "0.1.0",
  "description": "CUBELAND - a pocket-sized voxel sandbox. Seed-driven worlds, day/night cycle, crafting, smelting, mobs.",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^5.4.10"
  }
}
